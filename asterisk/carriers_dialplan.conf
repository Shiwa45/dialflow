; Auto-generated by DialFlow Pro
; Do not edit manually. Edit the Carrier 'dialplan_code' via Web UI.

; Carrier: openvox-out
[carrier_openvox-out]
exten => _X.,1,NoOp(OpenVox outbound to ${EXTEN})
 same => n,Set(__CAMPAIGN_ID=${CAMPAIGN_ID})
 same => n,Set(__LEAD_ID=${LEAD_ID})
 same => n,Set(__CUSTOMER_NUMBER=${CUSTOMER_NUMBER})
 same => n,Set(__CALL_TYPE=${CALL_TYPE})

 same => n,ExecIf($["${RECORD_CALL}"="1"]?MixMonitor(${STRFTIME(${EPOCH},,%Y/%m/%d)}/${UNIQUEID}-${LEAD_ID}.wav,b))

 same => n,ExecIf($["${AMD_ENABLED}"="1"]?AMD(${AMD_INITIAL_SILENCE},${AMD_GREETING},${AMD_AFTER_GREETING_SILENCE},${AMD_TOTAL_ANALYSIS_TIME},${AMD_MIN_WORD_LENGTH},${AMD_BETWEEN_WORDS_SILENCE},${AMD_MAX_WORDS},${AMD_SILENCE_THRESHOLD}))
 same => n,ExecIf($["${AMD_ENABLED}"="1" & "${AMDSTATUS}"="MACHINE"]?Hangup())

 same => n,Dial(PJSIP/${EXTEN}@carrier_openvox_out,60)
 same => n,Hangup()

